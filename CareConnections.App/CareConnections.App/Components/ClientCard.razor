<div class="col">
    <div class="card rounded-3 client-card">
        <div id="cardheader" class="card-header py-3">
            <h4> @Client.FirstName @Client.LastName</h4>
        </div>
        <div class="card-body client-card text-center">
            <div id="cardpicture" class="mb-3">
                <img src="@($"images/{Client.LastName}.jpg")" class="client-img rounded-circle img-fluid" />
            </div>
            <button id="detailsnavbtn" class="btn btn-outline-primary btn-sm mb-1"
                    @onclick="@(async () => await ClientDetailsClicked.InvokeAsync(Client))">
                Client Details
            </button>
            <button id="reportsnavbtn" class="btn btn-outline-primary btn-sm mb-1"
                    @onclick="(() => NavigateToReports(Client))">
                Client Reports
            </button>
        </div>
    </div>
</div>