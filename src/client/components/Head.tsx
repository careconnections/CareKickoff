import * as HeadTag from "next/head";

export const Head = () => (
	<HeadTag.default>
		<title>Care connections kick off</title>
		<meta name="description" content="Care connections kick off" />
		<link rel="icon" href="/favicon.ico" />
	</HeadTag.default>
);
